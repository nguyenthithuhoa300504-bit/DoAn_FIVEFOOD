const { DatabaseService } = require('./src/database/database.service.ts');
// wait, we can't easily require ts in plain node, let's use the same way they run tests.
// Let's see what's in test_logs2.js
